#include<conio.h>
#include<stdio.h>
#include<string.h>
main()
{
   char *str1,*str2;
   int i;
   gets(str2);
   for(i=0;str2[i]!='\0';i++)
   {str1=str2;
   }
    puts(str1);
    printf("%d",i);
    getch();
}